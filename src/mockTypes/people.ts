import tagRatingMock from "./tagRating";
import eventTagMock from "./eventTag";

const peopleMock: IPeople = {
    name: "",
    age: 0,
    tags: [tagRatingMock],
    history: [eventTagMock],
};

export default peopleMock;
