import pointMock from "./point";

const pointAggregationMock: IPointAggregation = {
    alpha: [pointMock],
    beta: [pointMock],
    gamma: [pointMock],
    theta: [pointMock],
};

export default pointAggregationMock;
